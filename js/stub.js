/**
 * Created by bcouriol on 7/06/14.
 */

/*
function stub(url_param, url_log) {
   logEntry("stub");
   var my_url = $("#" + url_param + " option:selected").val();
   $("#" + url_log).text(my_url);
   make_article_readable(my_url, activate_read_words_over);
   logWrite(DBG.TAG.DEBUG, "url", my_url);
   logExit("stub");
}

$(function () {
   $("#url_param").change(function () {
      stub("url_param", "url");
   });
});

 */
